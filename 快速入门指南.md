# RLPixTuner 推理快速入门（5分钟上手）

## 🎯 核心概念

RLPixTuner 是一个**目标条件**的照片后期处理模型，需要同时提供：
- **输入图像**: 待处理的原始照片
- **目标图像**: 期望效果的参考照片（如专业摄影师修图后的效果）

模型会自动调整 ISP 参数，使输入图像接近目标效果。

---

## 🚀 三种使用方式

### 方式 1: 命令行工具（最简单）⭐

**适合**: 快速测试单张图片

```bash
python inference_single_image.py \
    --input 输入图片.jpg \
    --target 目标图片.jpg \
    --output_dir 输出目录
```

**完整示例**:
```bash
python inference_single_image.py \
    --input examples/input.jpg \
    --target examples/target.jpg \
    --model_path envs/checkpoints/best_model.zip \
    --output_dir ./results \
    --isp full \
    --max_step 10
```

**输出**: 在 `./results/` 目录生成 `output.png`

---

### 方式 2: Python 一行代码（最快捷）⚡

**适合**: 在 Python 脚本中快速使用

```python
from rlpixtuner_api import quick_inference

metrics = quick_inference(
    '输入图片.jpg',
    '目标图片.jpg',
    '输出图片.png'
)

print(f"PSNR: {metrics['psnr']:.2f} dB")
```

---

### 方式 3: Python API（最灵活）🔧

**适合**: 需要批量处理或自定义功能

```python
from rlpixtuner_api import RLPixTunerInference

# 1. 创建推理器（只需创建一次）
inferencer = RLPixTunerInference(
    model_path='envs/checkpoints/best_model.zip',
    isp_type='full',  # 'wb' / 'exp-wb-cont' / 'full'
    max_step=10
)

# 2. 单张推理
output, metrics = inferencer.predict(
    '输入1.jpg',
    '目标1.jpg',
    '输出1.png'
)
print(f"PSNR: {metrics['psnr']:.2f} dB")

# 3. 批量推理
image_pairs = [
    ('输入1.jpg', '目标1.jpg'),
    ('输入2.jpg', '目标2.jpg'),
    ('输入3.jpg', '目标3.jpg'),
]

results = inferencer.predict_batch(
    image_pairs,
    output_dir='结果目录'
)

# 4. 提取 ISP 参数
params = inferencer.get_isp_parameters()
print(f"曝光参数: {params['ExposureFilter']}")
```

---

## 📦 环境安装

### 方法 1: 使用 pip（推荐）

```bash
pip install torch==2.0.0 torchvision==0.15.0
pip install stable-baselines3 gymnasium opencv-python pillow numpy
pip install easydict
```

### 方法 2: 使用 conda

```bash
conda create --name rlpixtuner --file requirements.txt
conda activate rlpixtuner
```

---

## 📂 准备数据

### 单张图片推理

只需准备两张图片：

```
your_folder/
├── input.jpg    # 输入图像
└── target.jpg   # 目标参考图像
```

### 批量推理

按以下格式组织：

```
dataset/
└── val/
    ├── image001-Input.jpg
    ├── image001-Target.jpg
    ├── image002-Input.jpg
    ├── image002-Target.jpg
    └── ...
```

**命名规则**: 必须以 `-Input` 和 `-Target` 结尾，前缀需匹配。

---

## 🎨 ISP 流程选择

| 类型 | 参数数量 | 滤镜组合 | 适用场景 |
|-----|---------|---------|---------|
| **wb** | 3个 | 白平衡 | 仅需色温调整 |
| **exp-wb-cont** | 5个 | 曝光+白平衡+对比度 | 基础调整 |
| **full** ⭐ | 10个 | 完整7滤镜 | 完整后期处理 |

**推荐**: 使用 `full` 获得最佳效果。

---

## ⚙️ 常用参数

### 命令行参数

```bash
--input          # 输入图像路径（必需）
--target         # 目标图像路径（必需）
--model_path     # 模型路径（默认: envs/checkpoints/best_model.zip）
--output_dir     # 输出目录（默认: ./inference_output）
--isp            # ISP流程: wb / exp-wb-cont / full（默认: full）
--max_step       # 最大优化步数（默认: 10，推荐: 5-10）
```

### Python API 参数

```python
RLPixTunerInference(
    model_path='模型路径',
    isp_type='full',        # wb / exp-wb-cont / full
    max_step=10,            # 优化步数（5-10）
    device='cuda',          # cuda / cpu
    verbose=True            # 是否打印详细信息
)
```

---

## 📊 输出内容

### 图像文件

```
output_dir/
├── input.png     # 输入图像（副本）
├── target.png    # 目标图像（副本）
└── output.png    # 处理结果（最佳效果）
```

### 评估指标

```python
metrics = {
    'psnr': 32.45,    # 峰值信噪比（越高越好，通常 25-40）
    'ssim': 0.95,     # 结构相似度（0-1，越高越好）
    'steps': 8        # 实际优化步数
}
```

---

## 💡 使用技巧

### 1. 如何选择目标图像？

✅ **好的目标图像**:
- 专业摄影师修图后的照片
- 风格一致的参考照片
- 后期处理合理的图像

❌ **不好的目标图像**:
- 过度HDR或过度锐化
- 风格差异太大
- 严重失真的图像

### 2. 优化步数怎么选？

- **5 步**: 快速预览（约 2-3 秒）
- **10 步**: 标准质量（约 5-10 秒）⭐ 推荐
- **15 步**: 极致质量（约 15-20 秒）

通常 10 步已经能达到很好的效果。

### 3. GPU vs CPU？

- **GPU**: 约 0.5-1 秒/步
- **CPU**: 约 2-5 秒/步

推荐使用 GPU，速度快约 10 倍。

---

## 🔍 实战示例

### 示例 1: 处理一张照片

```bash
# 命令行方式
python inference_single_image.py \
    --input photo.jpg \
    --target reference.jpg \
    --output_dir results
```

```python
# Python 方式
from rlpixtuner_api import quick_inference

quick_inference('photo.jpg', 'reference.jpg', 'output.png')
```

### 示例 2: 批量处理旅行照片

```python
from rlpixtuner_api import RLPixTunerInference

inferencer = RLPixTunerInference('envs/checkpoints/best_model.zip')

# 准备照片对
photos = [
    ('旅行照片1.jpg', '参考风格1.jpg'),
    ('旅行照片2.jpg', '参考风格2.jpg'),
    ('旅行照片3.jpg', '参考风格3.jpg'),
]

# 批量处理
results = inferencer.predict_batch(
    photos,
    output_dir='旅行照片处理结果',
    save_comparison=True  # 生成对比图
)

# 查看结果
for r in results:
    print(f"{r['input_path']}: PSNR={r['metrics']['psnr']:.2f} dB")
```

### 示例 3: 调整不同风格

```python
# 尝试不同的 ISP 流程
isp_types = ['wb', 'exp-wb-cont', 'full']

for isp in isp_types:
    inferencer = RLPixTunerInference(
        'envs/checkpoints/best_model.zip',
        isp_type=isp
    )
    
    output, metrics = inferencer.predict(
        'input.jpg',
        'target.jpg',
        f'output_{isp}.png'
    )
    
    print(f"{isp}: PSNR={metrics['psnr']:.2f} dB")
```

---

## ❓ 常见问题

### Q1: 必须提供目标图像吗？

**是的**。RLPixTuner 是目标条件模型，需要参考图像来指导优化方向。

### Q2: 可以处理任意尺寸的图像吗？

**可以**。模型会自动调整图像尺寸，但会保持宽高比。

### Q3: 推理需要多长时间？

- GPU: 5-10 秒（10步）
- CPU: 20-50 秒（10步）

### Q4: 如何提高处理速度？

1. 使用 GPU
2. 减少 `max_step` 参数（如改为 5）
3. 批量处理时复用 inferencer 对象

### Q5: 报错 "CUDA out of memory"

```python
# 方案1: 使用 CPU
inferencer = RLPixTunerInference(..., device='cpu')

# 方案2: 降低图像尺寸（修改 envs/run_sb3_eval.py）
--env_img_sz 32  # 默认 64
```

---

## 📚 更多资源

- **完整文档**: `INFERENCE_GUIDE.md`
- **API 参考**: `rlpixtuner_api.py`（查看 docstring）
- **使用示例**: `example_usage.py`
- **部署说明**: `DEPLOYMENT_README.md`

---

## 🎉 快速开始检查清单

- [ ] 安装依赖（torch, stable-baselines3 等）
- [ ] 确认模型文件存在（`envs/checkpoints/best_model.zip`）
- [ ] 准备输入和目标图像
- [ ] 运行第一个推理测试
- [ ] 查看输出结果

```bash
# 一键测试（确保有测试图片）
python inference_single_image.py \
    --input test_input.jpg \
    --target test_target.jpg \
    --output_dir test_results
```

**成功标志**: 在 `test_results/` 看到 `output.png` 文件

---

**恭喜！您已经掌握了 RLPixTuner 的基本使用方法！** 🎊

有问题？查看 `INFERENCE_GUIDE.md` 获取更多帮助。
