# RLPixTuner 模型参数分度值详解

## 📐 分度值定义

模型输出的调整参数分度值指的是**参数调整的最小精度**，即参数可以调整的最小步长。

---

## 🎯 核心机制

### 1. 模型输出格式

```python
# 模型输出
action = model.predict(observation)  # 输出类型: numpy.ndarray, dtype=float32
# 输出范围: [-1.0, 1.0]
# 维度: 取决于 ISP 滤镜数量
```

### 2. 参数转换公式

```python
# 代码位置: envs/isp_env.py, line 340
actual_param = range_l + (range_r - range_l) * (action + 1) / 2
```

**公式说明**:
- `action`: 模型输出的归一化值 ∈ [-1, 1]
- `range_l`: 滤镜参数的最小值
- `range_r`: 滤镜参数的最大值
- `actual_param`: 实际应用的参数值

---

## 🔬 理论分度值

### 计算基础

模型使用 **float32** 浮点数，其特性：

```python
import numpy as np

# float32 精度
epsilon = np.finfo(np.float32).eps
print(f"float32 机器精度: {epsilon}")  # ≈ 1.192e-07

# 在 [-1, 1] 范围内的理论最小步长
theoretical_resolution = epsilon * 2  # ≈ 2.384e-07
```

### 理论分度值

| 项目 | 值 |
|-----|-----|
| 数据类型 | float32 |
| 机器精度 (ε) | ~1.19 × 10⁻⁷ |
| 动作空间范围 | [-1, 1] |
| **理论最小分度值** | ~**2.4 × 10⁻⁷** |

---

## 📊 各滤镜参数的实际分度值

### Full ISP 流程的7个滤镜

| 滤镜名称 | 参数数量 | 参数范围 | 范围宽度 | 实际分度值* | 说明 |
|---------|---------|---------|---------|-----------|------|
| **Exposure** | 1 | [-2.0, 2.0] | 4.0 | ~**9.5×10⁻⁷** | 曝光调整 |
| **White Balance** | 3 | [0.606, 1.649] | 1.043 | ~**2.5×10⁻⁷** | R/G/B 通道 |
| **Saturation** | 1 | [-1.0, 1.0] | 2.0 | ~**4.8×10⁻⁷** | 饱和度 |
| **Contrast** | 1 | [-1.0, 1.0] | 2.0 | ~**4.8×10⁻⁷** | 对比度 |
| **Highlight** | 1 | [-1.0, 1.0] | 2.0 | ~**4.8×10⁻⁷** | 高光调整 |
| **Shadow** | 1 | [-1.0, 1.0] | 2.0 | ~**4.8×10⁻⁷** | 阴影调整 |
| **Sharpen** | 1 | [0.0, 2.0] | 2.0 | ~**4.8×10⁻⁷** | 锐化强度 |

\* 实际分度值 = 理论分度值 × 范围宽度 = 2.4×10⁻⁷ × (range_r - range_l)

### 计算示例

**曝光参数 (Exposure)**:
```python
range_l = -2.0
range_r = 2.0
range_width = 4.0

# 模型输出 action = 0.5 (范围 [-1, 1])
actual_param = -2.0 + 4.0 * (0.5 + 1) / 2
            = -2.0 + 4.0 * 0.75
            = 1.0

# 理论最小调整步长
min_step = 2.4e-7 * 4.0 = 9.6e-7
```

**白平衡参数 (White Balance)**:
```python
# 使用对数空间
log_wb_range = 0.5
range_l = exp(-0.5) ≈ 0.6065
range_r = exp(0.5) ≈ 1.6487
range_width ≈ 1.043

# 理论最小调整步长
min_step = 2.4e-7 * 1.043 ≈ 2.5e-7
```

---

## 🎨 实际应用中的分度值

### 1. 有效分度值

虽然理论分度值极小（~10⁻⁷ 级别），但在实际应用中，**有效分度值**受以下因素限制：

#### (1) 神经网络输出精度

```python
# TD3 算法的 actor 网络输出经过 tanh 激活
# 实际输出精度受网络权重量化影响
# 有效精度约为: 1e-5 到 1e-6
```

#### (2) 图像显示精度

```python
# 8-bit 图像: 256 级 (0-255)
# 每个通道的最小变化: 1/255 ≈ 0.004 (0.4%)

# 例如曝光调整
exposure_change = 0.01  # 对应像素值变化约 2-3 个灰度级
# 小于 0.001 的曝光变化在 8-bit 图像中几乎不可见
```

#### (3) 人眼感知极限

```python
# Just Noticeable Difference (JND)
# 亮度 JND: 约 1-2%
# 色彩 JND: 约 2-3 个 CIE ΔE 单位

# 实际有意义的参数变化:
exposure_jnd ≈ 0.02      # 曝光
saturation_jnd ≈ 0.03    # 饱和度
wb_jnd ≈ 0.01            # 白平衡
```

### 2. 有效分度值总结

| 滤镜参数 | 理论分度值 | 网络有效精度 | 图像显示精度 | 人眼感知精度 | **实际有效分度值** |
|---------|-----------|------------|------------|------------|------------------|
| Exposure | 9.6×10⁻⁷ | ~1×10⁻⁵ | ~0.004 | ~0.02 | **0.01 - 0.02** |
| White Balance | 2.5×10⁻⁷ | ~1×10⁻⁵ | ~0.004 | ~0.01 | **0.005 - 0.01** |
| Saturation | 4.8×10⁻⁷ | ~1×10⁻⁵ | ~0.004 | ~0.03 | **0.01 - 0.03** |
| Contrast | 4.8×10⁻⁷ | ~1×10⁻⁵ | ~0.004 | ~0.02 | **0.01 - 0.02** |
| Highlight | 4.8×10⁻⁷ | ~1×10⁻⁵ | ~0.004 | ~0.02 | **0.01 - 0.02** |
| Shadow | 4.8×10⁻⁷ | ~1×10⁻⁵ | ~0.004 | ~0.02 | **0.01 - 0.02** |
| Sharpen | 4.8×10⁻⁷ | ~1×10⁻⁵ | ~0.004 | ~0.05 | **0.02 - 0.05** |

---

## 🔍 验证方法

### 方法1: 输出参数值

```python
from rlpixtuner_api import RLPixTunerInference

inferencer = RLPixTunerInference('envs/checkpoints/best_model.zip')
output, metrics = inferencer.predict('input.jpg', 'target.jpg')

# 获取 ISP 参数
params = inferencer.get_isp_parameters()

for filter_name, param_value in params.items():
    print(f"{filter_name}:")
    print(f"  值: {param_value}")
    print(f"  精度: {param_value.dtype}")
    print()
```

**输出示例**:
```
ExposureFilter:
  值: [0.15234567]
  精度: float32

ImprovedWhiteBalanceFilter:
  值: [1.05123456 0.98765432 1.02345678]
  精度: float32

SaturationFilter:
  值: [0.12345678]
  精度: float32
```

### 方法2: 对比连续两次推理

```python
import numpy as np

# 多次推理同一张图片（使用 deterministic=True）
results = []
for i in range(10):
    output, metrics = inferencer.predict('input.jpg', 'target.jpg')
    params = inferencer.get_isp_parameters()
    results.append(params)

# 计算参数稳定性
exposure_values = [r['ExposureFilter'][0] for r in results]
print(f"曝光参数稳定性: {np.std(exposure_values)}")
# 输出: ~0 (deterministic 模式下应该完全一致)
```

### 方法3: 参数扰动测试

```python
# 测试最小可感知的参数变化
from isp_blocks import ISPBlocks
from config import cfg

cfg.custom_isp = [ExposureFilter]
isp = ISPBlocks(cfg, is_blackbox=True)
isp.init_filters(cfg.custom_isp)

import torch
input_img = torch.rand(1, 3, 256, 256)

# 测试不同的曝光值
exposures = [0.0, 0.001, 0.005, 0.01, 0.05, 0.1]
for exp in exposures:
    param = torch.tensor([[exp]])
    output = isp.run(input_img, [param])
    diff = torch.mean(torch.abs(output - input_img)).item()
    print(f"曝光 {exp}: 平均像素差 {diff:.6f}")
```

---

## 💡 实际应用建议

### 1. 参数调整粒度

| 应用场景 | 推荐分度值 | 原因 |
|---------|-----------|------|
| **自动后期处理** | 模型默认 | 模型自动优化，无需人工干预 |
| **手动微调** | 0.01 - 0.05 | 符合人眼感知和UI控制 |
| **快速预览** | 0.05 - 0.1 | 减少计算量，提高响应速度 |
| **精细调整** | 0.001 - 0.01 | 专业后期处理需求 |

### 2. UI 设计建议

如果要设计基于此模型的后期处理 UI：

```python
# 滑块配置示例
slider_configs = {
    'Exposure': {
        'range': [-2.0, 2.0],
        'step': 0.01,          # 建议步长
        'precision': 2,        # 显示精度（小数点后2位）
    },
    'White Balance': {
        'range': [0.6, 1.65],
        'step': 0.01,
        'precision': 2,
    },
    'Saturation': {
        'range': [-1.0, 1.0],
        'step': 0.01,
        'precision': 2,
    },
    # ... 其他参数
}
```

### 3. 性能优化

```python
# 如果需要加速推理，可以量化参数输出
def quantize_params(params, precision=0.01):
    """
    将参数量化到指定精度
    
    Args:
        params: 原始参数
        precision: 量化精度（默认 0.01）
    
    Returns:
        量化后的参数
    """
    return np.round(params / precision) * precision

# 使用示例
raw_params = inferencer.get_isp_parameters()
quantized_params = {
    k: quantize_params(v, precision=0.01) 
    for k, v in raw_params.items()
}
```

---

## 📈 参数精度对 PSNR 的影响

### 实验数据（理论估算）

| 参数精度 | PSNR 损失 | 视觉差异 | 适用场景 |
|---------|----------|---------|---------|
| float32 (原始) | 0 dB | 无 | 标准推理 |
| 0.001 | < 0.01 dB | 几乎无法察觉 | 精细调整 |
| 0.01 | < 0.1 dB | 肉眼难以察觉 | 日常使用（推荐）✅ |
| 0.05 | ~0.2-0.5 dB | 轻微可见 | 快速预览 |
| 0.1 | ~0.5-1.0 dB | 明显可见 | 不推荐 |

---

## 🎯 总结

### 关键结论

1. **理论分度值**: ~**2.4 × 10⁻⁷** (float32 精度)

2. **实际有效分度值**: ~**0.01 - 0.05** (受网络、显示和感知限制)

3. **推荐使用分度值**: **0.01** (平衡精度和性能)

4. **模型特点**:
   - ✅ 连续参数空间（非离散）
   - ✅ 高精度输出（float32）
   - ✅ 可微分（支持梯度优化）
   - ✅ 端到端学习

### 相比传统方法的优势

| 维度 | 传统方法 | RLPixTuner |
|-----|---------|-----------|
| 参数类型 | 离散（手动档位） | 连续（无级调整）|
| 分度值 | 0.1 - 1.0 | ~10⁻⁷ |
| 调整精度 | 粗糙 | 极高 |
| 优化速度 | 慢（500+ 步）| 快（10 步）|
| 参数空间 | 有限组合 | 连续空间 |

---

## 🔬 进阶: 查看原始模型输出

```python
# 查看模型的原始输出（未转换前的 action）
import numpy as np
from envs.custom_td3 import CustomTD3

model = CustomTD3.load('envs/checkpoints/best_model.zip', env=env)
obs, info = env.reset()

# 获取原始 action（[-1, 1] 范围）
action, _ = model.predict(obs, deterministic=True)

print("原始动作输出:")
print(f"  类型: {action.dtype}")
print(f"  形状: {action.shape}")
print(f"  范围: [{action.min():.8f}, {action.max():.8f}]")
print(f"  值: {action}")

# 查看精度
unique_decimals = len(str(action[0]).split('.')[1])
print(f"  有效小数位: ~{unique_decimals}")
```

---

## 📚 参考资料

- 模型架构: TD3 (Twin Delayed Deep Deterministic Policy Gradient)
- 动作空间: Box(low=-1, high=1, shape=(n,))
- 参数转换: 线性映射
- 代码位置: `envs/isp_env.py`, line 330-343

---

**总结**: RLPixTuner 使用 **float32 连续动作空间**，理论分度值为 **~2.4×10⁻⁷**，但实际应用中推荐使用 **0.01** 作为有效分度值，既保证了调整精度，又符合人眼感知和实际应用需求。
